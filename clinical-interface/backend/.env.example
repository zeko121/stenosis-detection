# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=Coronary Stenosis Detection System
DEBUG=False
RELOAD=False

# Server
HOST=0.0.0.0
PORT=8000

# Database
DATABASE_URL=sqlite+aiosqlite:///./stenosis_clinical.db

# Redis & Celery
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Model Paths (update with actual paths)
SEGMENTATION_MODEL_PATH=../../models/coronary_segmentation/checkpoints/best_model.pth
CLASSIFICATION_MODEL_PATH=../../models/coral_classification/checkpoints/best_model.pth

# Processing Configuration
DEVICE=cuda
MAX_UPLOAD_SIZE_MB=500
BATCH_SIZE=1
ENABLE_MIXED_PRECISION=True

# Preprocessing
HU_WINDOW_MIN=-100
HU_WINDOW_MAX=1000
TARGET_SPACING=0.8

# Security (CHANGE IN PRODUCTION!)
SECRET_KEY=CHANGE_THIS_GENERATE_WITH_openssl_rand_hex_32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS Origins (comma-separated)
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]
